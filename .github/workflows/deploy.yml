name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
<<<<<<< HEAD

    env:
      OPENWEATHER_KEY: ${{ secrets.OPENWEATHER_KEY}}
      RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY}}

=======
    
    env:
        OPENWEATHER_KEY: ${{ secrets.OPENWEATHER_KEY}}
        RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY}}
    
>>>>>>> c71fb7a490d18b6ac9b4c884e3845f512d1b6408
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: 16

      - name: Install dependencies
        uses: bahmutov/npm-install@v1

      - name: Generate Environment File
        run: |
    echo "export const environment = {" > env.js
    echo "  VITE_OPENWEATHER_KEY: '${OPENWEATHER_KEY}'," >> env.js
    echo "  VITE_RAPIDAPI_KEY: '${RAPIDAPI_KEY}'" >> env.js
    echo "};" >> env.js
      - name: Build project
        run: npm run build
<<<<<<< HEAD
=======
        
      - name: Print Secrets
        run: |
          echo "OpenWeather Key: $OPENWEATHER_KEY"
          echo "RapidAPI Key: $RAPIDAPI_KEY"
>>>>>>> c71fb7a490d18b6ac9b4c884e3845f512d1b6408

      - name: Upload production-ready build files
        uses: actions/upload-artifact@v2
        with:
          name: production-files
          path: ./dist

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: production-files
          path: ./dist

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
